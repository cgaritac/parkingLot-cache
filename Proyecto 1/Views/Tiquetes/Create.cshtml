@model Proyecto_1.Models.Tiquete

@{
    ViewData["Title"] = "Create";
}

<h1 style="text-align:center">AGREGAR TIQUETE</h1>

<h2>Datos del Tiquete</h2>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group mb-3">
                <strong>
                    @Html.DisplayNameFor(model => model.Id):
                </strong>
                @Html.DisplayFor(model => model.Id)
            </div>
            <div class="form-group">
                <strong>
                    <label asp-for="Ingreso" class="control-label"></label>
                </strong>
                <input asp-for="Ingreso" class="form-control" type="datetime-local" />
                <span asp-validation-for="Ingreso" class="text-danger"></span>
            </div>
            <div class="form-group">
                <strong>
                    <label asp-for="Placa" class="control-label"></label>
                </strong>
                <input asp-for="Placa" class="form-control" />
                <span asp-validation-for="Placa" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="hidden" asp-for="Estado" value="Abierto" />
            </div>
            <div class="form-group mt-4">
                <input type="submit" value="Agregar" class="btn btn-primary" /> |
                <a asp-action="Index">Regresar</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
